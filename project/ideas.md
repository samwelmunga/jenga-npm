# Ideas

<!-- Format: <idea text> (one line per idea; terminal tags PROMOTED / DISCARDED appended by /brainstorm, not by /idea) -->
Investigate concurrent-write race on project/queue/.session_handoff.json when multiple developer/tester sessions run in parallel (Origin: E35_S01_T03 — surfaced by tester during /jenga e35 rollout, one handoff clobbered another before on_session_end.sh could consume it) PROMOTED → E37_S01 (2026-08-25)
Replace /commit's narrative changed-file-detection for doc-sync's `source:` scoping with a single canonical script (similar to other skills' script-offload pattern) for determinism across agent sessions, and define a one-line prefix/header convention so doc-sync's folded report is visually distinguishable from /commit's own output (Origin: E28_S03_T02 — tester rapport project/rapports/problems/E28_S03_T02-changed-file-detection-narrative-and-no-report-format.md, both findings low severity/non-blocking, deferred rather than addressed in-task) (2026-08-25)
Add appended-scope support to /commit (e.g. /commit E46) so its mandatory /reconcile step passes that item's scope through to /reconcile instead of invoking it unscoped (Origin: E46 — /commit E46 triggered a full unscoped reconcile across 46 epics/194 stories/403 tasks, flagged as disproportionate) DONE → E17_S07 (/reconcile's own scope argument + default-scope-to-epic rule) + E17_S04_T02 (/commit passes its scope through) (2026-09-01)
Investigate a 'last reconciled' bookkeeping marker (SHA/timestamp, likely in project/queue/) so a routine /reconcile run can check only what changed since last time instead of always being O(board size) (Origin: E46, split off from the /commit-scope-passthrough idea above once that half landed as E17_S07/E17_S04_T02) (2026-09-01)
Add two deterministic, script-set board statuses: "Merged" (set after /self-sync completes successfully, for tickets mirrored into .claude/ and .agents/) and "Deployed" (set after /mirror-public completes successfully, for tickets shipped to the public mirror) — both belong in one status-lifecycle-automation story, not agent-judged, likely needs SCRUM_BOARD_SCHEMA.md + validate-board.sh + workflow.json updates and open questions on task-vs-story/epic scope and Merged-vs-Deployed ordering (Origin: E06 — surfaced while scoping the E06 dashboard kanban-column /todo mission, which separately needs a "Deployed" UI column once this status exists) PROMOTED → E51 (grew to 5 statuses: Merged, Publicized, Privatized, Deployed to Stage, Deployed to Prod — "Deployed" split in two once its /mirror-public trigger turned out to only mean "shipped to the public mirror repo," not an actual npm/prod release) (2026-09-02)
Widen /do's story-bundle grouping beyond same-story membership to depends_on-linked tasks, so a subagent can handle a coherent group of related tasks spanning multiple stories in one worktree, not just tasks under the same story — contingent on first hardening `depends_on` from advisory-only (per templates/SCRUM_BOARD_SCHEMA.md) to a reliably-populated field, since a bundling decision built on sparse advisory data would silently treat genuinely related tasks as independent (Origin: E32_S15 /brainstorm on 2026-09-03 — a paired idea proposing subagent self-directed mid-task context-forgetting was evaluated in the same session and dropped outright as too risky: no way for a subagent to know in advance what a later task in the group will need, unlike E49's resolved_context which is additive-only and safe because the receiver can always fall back to source files) (2026-09-03)
/jenga Phase 4's wave loop only rescans Pending items once every background agent in the current wave has completed, so a slot freed by an early finisher sits idle until the wave's slowest straggler also finishes — redesigning Phase 4 to backfill from Pending as soon as any single agent completes, rather than waiting for the whole wave to drain, would close this gap but is a bigger orchestration change than the concurrency-cap story it surfaced alongside (Origin: E32_S15 /brainstorm on 2026-09-03 — accepted as a known, unaddressed characteristic rather than folded into E32_S15's scope) (2026-09-03)
