# E06_S01_T04 Summary — Loading and Error Components

Created `LoadingSpinner.jsx` and `ErrorMessage.jsx` under `src/components/`. Both import a shared `components.css`. `LoadingSpinner` shows centred grey text while data is in flight; `ErrorMessage` normalises Error objects and strings into a red-tinted warning box with a ⚠ prefix.
