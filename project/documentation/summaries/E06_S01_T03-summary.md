# E06_S01_T03 Summary — Tab Structure

Built `App.jsx` with a static tabs configuration array and `useState` for active tab tracking. Renders `BoardTab`, `HistoryTab`, and `ArchitectureTab` conditionally. Styled the tab bar in `App.css` with an active blue underline indicator and clean sans-serif typography.
