# E06_S01_T03 Plan — Tab Structure

Implement `App.jsx` with a tabs array (Board, History, Architecture) and `useState` for the active tab. Render named tab components conditionally. Apply minimal CSS in `App.css` for the tab bar and active indicator.
