# E06_S01_T04 Plan — Loading and Error Components

Create `LoadingSpinner` and `ErrorMessage` reusable components under `src/components/`. Share a single `components.css` for their styles. `ErrorMessage` normalises Error objects and plain strings into a readable message.
