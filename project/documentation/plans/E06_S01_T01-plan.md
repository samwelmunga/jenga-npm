# E06_S01_T01 Plan — Scaffold Vite React App

Manually create `dashboard/` directory with a hand-written `package.json`, `vite.config.js`, and `index.html` (no interactive CLI). Install React 18 and `@vitejs/plugin-react`. Add `.env.example` with `VITE_API_BASE_URL` and a `.gitignore` excluding `node_modules/` and `dist/`.
