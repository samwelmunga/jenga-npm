# Execution Plan — E17_S02_T01

1. Review `templates/SCRUM_BOARD_SCHEMA.md` to identify the Epic, Story, and Task frontmatter examples and preserve all existing text.
2. Add `dates_previously_completed`, `reopened_on`, and `reopened_reason` immediately after `date_completed:` in each example, using inline comments that show comma-separated-list examples.
3. Add a clearly visible `Reopen Tracking Fields` note explaining these fields are only used when reopening previously completed items and must remain blank on first-run items.
4. Review the updated schema against the acceptance criteria to confirm no existing sections or wording were removed.
5. Write a summary, update task/story status metadata for this documentation-only task, and commit the changes with the requested EST message.
