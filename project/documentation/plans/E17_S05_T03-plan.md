# E17_S05_T03 — Execution Plan

## Task
Update `README.md` and `WARP.md` to document the new `/reconcile-origin` skill.

## Implementation Approach
1. Add `/reconcile-origin` to the README skills table in the appropriate section.
2. Inspect whether `WARP.md` already exists; if not, create it in a concise command-reference style that matches the repository's markdown conventions.
3. Add a dedicated `/reconcile-origin` section covering purpose, usage, conflict handling, and recommended use cases.

## Files to Change
| File | Planned Change |
|------|----------------|
| `README.md` | Add `/reconcile-origin` skills-table entry |
| `WARP.md` | Add or create `/reconcile-origin` documentation section |

## Dependencies & Risks
- `WARP.md` is currently absent in this repository snapshot, so documenting the skill may require creating the file.
- Documentation must stay consistent with the implemented script and skill contract.

## Notes
No board files will be modified.
