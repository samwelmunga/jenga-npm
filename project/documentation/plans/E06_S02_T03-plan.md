# E06_S02_T03 Plan — Board Layout

Create `BoardView.jsx` as the top-level board container. Add `board.css` with flex-column layout for the board, indented story/task lists, white card backgrounds with subtle borders, and all status badge colour utilities. Import `board.css` in `BoardView.jsx` to co-locate styles with the component tree.
