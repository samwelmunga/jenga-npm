# Execution Plan — E18_S03_T01

1. Update `project/.wiki/concepts/parallel-tasks.md` to mention `/jenga` alongside `/dooo` in the header callout while preserving the existing `/dooo`, `/reconcile`, and sub-agent-session guidance.
2. Add a new `/jenga` section describing it as the hands-free, board-wide alternative and explain Phase 4's dependency resolution, parallel batching, background sub-agent launches, and repeat-until-done loop.
3. Add a short `/dooo` vs `/jenga` comparison so the interactive versus fully automated distinction is explicit for first-time readers.
4. Update the `When Not to Use /dooo` guidance to point readers to `/jenga` when they want full-board automation without manual approvals.
5. Review the edited concept card for acceptance-criteria coverage, write the execution summary, update the task metadata, and commit with the requested message.
